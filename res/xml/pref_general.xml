<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android" >

    <!--
         NOTE: Hide buttons to simplify the UI. Users can touch outside the dialog to
         dismiss it.
    -->
    <!-- NOTE: ListPreference's summary should be set to its value by the activity code. -->
    <ListPreference
        android:defaultValue="Cape Breton"
        android:entries="@array/pref_region_list_titles"
        android:entryValues="@array/pref_region_list_values"
        android:key="region_list"
        android:negativeButtonText="@null"
        android:positiveButtonText="@null"
        android:summary="@string/pref_description_change_region"
        android:title="@string/pref_title_change_region" />

</PreferenceScreen>
